# YOLOForge Backend — Environment Variables
# Copy this file to .env and fill in values if needed

# Server
HOST=0.0.0.0
PORT=8000

# CORS — set to your frontend URL in production
CORS_ORIGINS=*

# Upload temp dir (optional, defaults to system temp)
# UPLOAD_DIR=/tmp/yoloforge_uploads
